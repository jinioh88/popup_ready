# Gradle 데몬이 실행될 JVM. 컴파일 toolchain(Java 25)과는 별개다.
#
# Spotless의 palantir-java-format이 javac 내부 API에 의존해 Java 25 JVM에서
# NoSuchMethodError로 깨지기 때문에 데몬만 21로 내린다. 포매터가 Java 25를
# 지원하면 이 파일을 지우고 데몬도 25로 되돌린다.
toolchainVersion=21
