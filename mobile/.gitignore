# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# 에이전트 로컬 자산 (git 비공유, 2026-08-23 사용자 결정)
# 슬래시를 붙이지 않는다 — `docs/`는 디렉터리만 매칭해서, worktree에서
# 원본 체크아웃을 가리키는 심볼릭 링크(= git에겐 파일)가 안 걸린다.
.claude
docs
/CLAUDE.md
